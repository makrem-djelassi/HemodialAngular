import { EtattostringPipe } from './etattostring.pipe';

describe('EtattostringPipe', () => {
  it('create an instance', () => {
    const pipe = new EtattostringPipe();
    expect(pipe).toBeTruthy();
  });
});
